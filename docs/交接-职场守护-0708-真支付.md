# 职场守护 交接 · 0708 真支付上线

## 📋 提交清单（全部已 push）
| 仓库 | commit | 内容 |
|---|---|---|
| zhichang-shouhu (github.com/karentan30/zhichang-shouhu · main) | `973d900` | 记录页拆日常循环（吐槽为主·取证折叠） |
| 同上 | `a6ffcbd` | ¥39 维权包真支付（微信/支付宝扫码·卡待Stripe） |
| 同上 | `929cb27` | 本交接文档 |
| lumee (github.com/karentan30/lumee · test/critical-paths) | `381f7cf` | server.py 加 zhichang_39 商品 + 匿名下单白名单 |

**部署状态**：guardian 前端已 `vercel --prod` 上线 `zhichang-shouhu.vercel.app/app`；Lumee server.py 已 scp+`systemctl restart` 上线 47.242.80.65 并验证仍在线（`ANON_PRODUCTS` 3 处·匿名建单返真 `weixin://` 二维码）。桌面源 `职场守护-app.html` 已同步。


## 本次做完（待办#1 + #2）

### 1. 记录页「拆日常循环」重设计（commit 973d900）
- 主界面改成 **"今天憋了什么？"大文本框当主角**，一句吐槽即可保存（`saveRec` 本就只校验 desc，类型缺省"其他"、严重缺省 med）
- 类型/时间人物/严重程度/证据 四组取证字段收进**默认折叠**的「＋ 补充取证细节（可选）」，保存后自动收起，中英同步

### 2. ¥39 维权包「真支付」上线（前端 a6ffcbd + 后端 Lumee 381f7cf）
**架构：复用 Lumee 的微信/支付宝商户号收款（香港公司户），职场守护前端跨域调 Lumee 后端。**

**前端（zhichang-shouhu repo · app.html）**
- `unlockPack()` 从"内测免费解锁"改为真收款：选微信/支付宝 → POST `https://www.mylumee.cn/pay/{wechat/create | alipay/qr}`（`productId:"zhichang_39"`，**匿名无 token**）→ 拿 `code_url`/`qr_code` → 用 `api.qrserver.com` 渲染成二维码图（长按识别）→ 每 3 秒轮询 `/pay/*/query?out_trade_no=` → `status==paid` 时置 `localStorage.zg_unlocked='1'` 并 `generateReport()` 生成案卷
- 银行卡 tab 显示"即将开放（接入 Stripe 中）·请先用微信/支付宝"
- `PAY_API = https://www.mylumee.cn`

**后端（Lumee server.py · 已部署 47.242.80.65 并验证）**
- `PRODUCT_PLAN_MAP['zhichang_39'] = ('zhichang', 39, '职场守护维权包')`
- `ANON_PRODUCTS = {'zhichang_39'}` + `_is_anon_product()`：**仅这一个商品**可免登录建单（微信/支付宝建单处 `if not user and not _is_anon_product(product): 401`）
- `_apply_paid_order` 对 `plan=='zhichang'` **空操作**：只收钱标记订单已付，**绝不给任何 Lumee 账号发会员/权益**（otoken 可能为空）
- 微信/支付宝支付描述用「职场守护 ·」前缀
- ✅ 安全兜底：白名单外/会员商品无 token **仍 401**；即便滥调最坏只产生"未支付空订单"，**无法白嫖 Lumee 会员**

## 线上验证（都过了）
- 匿名 `zhichang_39` 微信建单 → 返回真 `weixin://wxpay/bizpayurl?...` 二维码（`mock:false`，¥39）✅
- 会员商品 `treasure_monthly` 无 token → 401 ✅｜非白名单商品无 token → 401 ✅
- 前端 Playwright 端到端：弹窗/三 tab/真二维码/轮询/卡待开放/`onPaid` 解锁+生成案卷，0 报错 ✅

## ⚠️ 已知限制 / 待办
1. **收款商户名显示为「鹿觅/Lumee」**（复用 Lumee 商户号的必然代价）——用户付款时描述是"职场守护 · 维权包"但商户账户名是 Lumee。可接受则不动；要独立品牌需单独申请微信/支付宝商户。
2. **支付宝生产密钥未配**（`ALIPAY_APP_ID` 等未配）——现在点支付宝会返回后端的清晰 400"支付宝暂未开放，请用微信"。配 `ALIPAY_APP_ID`+私钥/公钥路径即自动可用（当面付代码就绪）。**微信已实测真收款可用。**
3. **Stripe（银行卡/海外）**：Karen 需先完成 Stripe 账号验证。验证后：slim-backend 已有 `lib/stripe.ts`+`api/stripe/checkout` 骨架可复用；给 zhichang 加一个 Stripe Checkout（支持 card + 可选 alipay/wechat_pay）serverless 函数，前端把银行卡 tab 接上即可。
4. QR 图目前走 `api.qrserver.com`（外部服务）——低风险，未来可内联 qrcodejs 去依赖。

## 部署方式（记牢·和"push自动部署"的误记不同）
- **Guardian 前端**：push GitHub **不会自动部署**！须 `cd 仓库 && vercel --prod --yes`（项目 `fabulousslim/zhichang-shouhu`）。app 在 `/app`（非根，根是落地页 index.html）。
- **Lumee server.py**：改前**先从服务器拉最新**（`ssh root@47.242.80.65 'cat /root/server.py' > server.py`），`python3 -m py_compile` → 备份 → `scp` → `systemctl restart xinshen.service` → `curl /ping`。`err(msg,code)` code≥500 会屏蔽自定义文案，用户向用 4xx。
